# 随拾 (SuiShi) · 后续迭代规划与开发路线图 (Roadmap)

> **定位宗旨**：坚守“零依赖、单文件、纯前端、100% 本地离线与温暖手账风”的轻量化核心。杜绝无意义的社交化、云端化与端侧重型 AI 等虚空需求，聚焦打磨**短视频翻阅体验**、**解码容错与性能抗压**、以及**轻量整理闭环**。

---

## 阶段规划概览

```
┌────────────────────────────────────────────────────────────────────────┐
│                        随拾 (SuiShi) 演进阶段矩阵                      │
├───────────────────┬───────────────────┬────────────────────────────────┤
│ 阶段              │ 核心目标          │ 关键特性                       │
├───────────────────┼───────────────────┼────────────────────────────────┤
│ P0: 核心播放与交互│ 消除硬伤，体验闭环│ - Feed 模式交互式进度条 (Seek) │
│     (即刻落地)    │ (可用 -> 好用)    │ - 快捷键全覆盖 & 倍速播放 (0.5~2x)│
│                   │                   │ - 原生全屏 (Fullscreen API)    │
│                   │                   │ - 目录一键 🔄 刷新与状态同步   │
│                   │                   │ - 格式不可播优雅降级与拦截提示 │
├───────────────────┼───────────────────┼────────────────────────────────┤
│ P1: 性能质变与记忆│ 秒级加载，省电顺滑│ - IndexedDB 缩略图持久化缓存池 │
│     (体验跃升)    │ (好用 -> 丝滑)    │ - 断点续播记忆系统 (Resume)    │
│                   │                   │ - 大目录分批渲染与内存抗压     │
├───────────────────┼───────────────────┼────────────────────────────────┤
│ P2: 轻量整理闭环  │ 挑选与除杂        │ - ⭐ 星标收藏 (Favorites)      │
│     (实用工具)    │ (观影 -> 整理)    │ - 废片快速标记与安全删除       │
└───────────────────┴───────────────────┴────────────────────────────────┘
```

---

## 优先级 P0：核心播放体验与交互闭环（消除硬伤）

### 1. Feed 模式交互式精细进度条 (Interactive Scrubber)
* **痛点现状**：沉浸式刷视频流中只有纯画面，面对 1~3 分钟的长片段时无法快进、倒回，无法感知剩余播放时长。
* **设计方案**：
  * 在每个 `.feed-slide` 底部上方（视频信息上方或底部贴边）叠加一条轻量沉浸式进度轨。
  * **常态**：高度 3px 的半透明焦糖色进度条，伴随 `timeupdate` 平滑过渡。
  * **悬停/触摸**：高度展开至 6px，浮现圆点滑块和时间浮层（`00:45 / 02:18`）。
  * **交互**：支持点击与拖拽跳转（Seek），拖拽过程中暂停自动播放，松手后跳至目标位置并继续播放。

### 2. 全功能播放控制与快捷键对齐
* **痛点现状**：Modal 播放器无倍速调节、缺少真全屏；Modal 模式下方向键上下闲置，缺乏直觉控制。
* **设计方案**：
  * **倍速调节**：
    * 提供快捷键 `[`（减速）与 `]`（加速），支持 `0.5x` / `0.75x` / `1.0x` / `1.25x` / `1.5x` / `2.0x`。
    * 画面中央短暂淡现当前倍速 Toast（例如 `⚡ 1.5x`），0.8 秒后淡出。
  * **真全屏支持**：
    * 引入 Fullscreen API：点击全屏按钮或按快捷键 `F` 触发 `container.requestFullscreen()`。
  * **Modal 快捷键统一规范**：
    * `Space`：切换播放 / 暂停。
    * `←` / `→`：快退 5 秒 / 快进 5 秒（若同时按住 `Shift` 则步进 10 秒）。
    * `↑` / `↓`：音量递增 / 递减 5%，中央浮现音量百分比 HUD。
    * `PageUp` / `PageDown` 或 `N` / `P`：切换至上一个 / 下一个视频文件。

### 3. 本地目录一键「🔄 重新扫描」
* **痛点现状**：Web API 无法监听文件系统底层变动，用户在外部修改、删除或下载新视频后，界面无法感知。
* **设计方案**：
  * 顶部工具栏搜索框旁增加「🔄 刷新」按钮，附带 Tooltip `刷新当前目录 (R)`。
  * 点击后重新遍历当前 `FileSystemDirectoryHandle`，对比内存列表做增量更新，并在状态栏显示 `扫描完毕：新增 X 个，移除 Y 个视频`。

### 4. 非原生格式优雅降级拦截（拒绝黑屏挫败感）
* **痛点现状**：列表识别了 `avi, mkv, wmv, flv`，但浏览器原生 `<video>` 无法解码，用户点击后直接黑屏转圈。
* **设计方案**：
  * **卡片视觉标识**：对非 `NATIVE_PLAYABLE`（`mp4`, `webm`, `mov`, `m4v`, `ogv`）的卡片，左上角角标标明格式并施加醒目区分（例如 `MKV·需外部播放`）。
  * **点击拦截弹窗**：点击此类卡片时，不启动原生 `<video>` 播放器，而是弹出轻量温馨提示框：
    > “当前视频为 **MKV** 格式，受现代浏览器内置解码器限制无法直接播放。  
    > 建议使用系统默认播放器（如 VLC、PotPlayer）打开。”
  * **实用小工具**：弹窗内提供「📋 复制文件完整相对路径」按钮，方便用户在本地播放器或命令行快速定位。

---

## 优先级 P1：性能质变与离线持久化（丝滑秒开）

### 1. IndexedDB 缩略图持久化缓存池 (Thumbnail Cache Pool)
* **痛点现状**：缩略图只保留在内存 Promise 中，刷新页面或切换目录后所有缩略图全部作废，必须重新抽取，风扇狂转且界面卡顿。
* **设计方案**：
  * 在 IndexedDB `suishi-video-db` 中新增对象存储空间 `thumbnails`。
  * **缓存键（Cache Key）**：采用哈希指纹 `${folderId}:${v.path}/${v.name}:${v.size}:${v.mtime}`，精准防脏。
  * **加载策略**：
    1. 渲染卡片时先命中 IDB 缓存，若存在则直接赋给 `<img>`（0ms 纯本地秒开海报墙）；
    2. 若未命中，进入原有离屏抽取管线，生成 320×180 JPEG 并在完成后异步写入 IDB。
  * **缓存管理**：在侧边栏底部提供「清理缩略图缓存」选项，防止长期使用占用过多浏览器本地存储。

### 2. 视频播放进度断点续播 (Resume Playback)
* **痛点现状**：长视频（素材长片、长 Vlog）关闭弹窗或刷新后进度丢失，下次需要手动拖拉寻找进度。
* **设计方案**：
  * 在 IDB 或 LocalStorage 中维护 `suishi_play_progress` 映射表：`[fileKey]: { time: number, duration: number, updatedAt: number }`。
  * 播放超过 5 秒且剩余时长大于 10 秒时自动记录进度。
  * 再次打开该视频时，如果记录未过期且未看完，自动从上次位置开始播放，并浮现短暂提示：`已为您恢复至 01:23 处播放`。

### 3. 大目录渲染抗压防卡顿 (Chunked / Virtual Rendering)
* **痛点现状**：当目录包含 1000+ 个视频时，直接在 DOM 树挂载数千个节点，导致页面初始化、搜索过滤重绘时产生明显顿挫。
* **设计方案**：
  * 保持原生单文件特质，引入分批分页渲染（Chunked Append / 懒分块），首屏仅挂载前 60 个卡片，用户滚动至底部 600px 附近时无感知追加后续分块。

---

## 优先级 P2：轻量整理能力（符合“抽屉”心智）

### 1. ⭐ 星标收藏与快速筛选 (Favorites)
* **痛点现状**：短视频素材浏览的核心诉求之一就是“挑出好看的”，目前没有任何标记手段。
* **设计方案**：
  * 卡片右上角及 Feed 模式 HUD 增加小星标（⭐）。
  * 状态持久化存入 IDB `favorites` 表中（记录 `folderId` 与相对路径）。
  * 顶部过滤胶囊栏固定第一项增加「⭐ 已收藏 (N)」，一键快速聚合并播放精华片段。

### 2. 废片安全删除与移出抽屉
* **痛点现状**：用户在整理手机导入的拍摄片段时，常有手抖、黑屏的废片，想顺手清理。
* **设计方案**：
  * 在卡片操作区与播放器中增加「🗑️ 删除文件」操作。
  * **权限安全**：触发时向用户申请 `createWritable` 写权限，并在删除前弹出二次防误触确认。
  * 删除成功后立即从本地磁盘抹除该文件，并同步更新列表与当前统计。

---

## 严格恪守的「防虚空需求」红线

在随拾后续任何阶段的代码演进中，**绝不引入**以下方向：
1. ❌ **不要引入 Node.js / Python 依赖型后端**：坚守静态双击即开或纯 `http-server` 托管的极简特性。
2. ❌ **不要引入第三方重型 UI 框架（React/Vue/Angular）**：继续保持无构建打包含税的单文件 HTML 纯粹性。
3. ❌ **不要引入端侧重型 AI 大模型剪辑**：避免引入几百兆的 WASM 模型占用内存和带宽。
4. ❌ **不要引入社交、公网云盘与外部分享机制**：恪守“100% 离线、私密安全”的个人工具护城河。

> **关于「🖼 看图」（v1.4 已落地）**：抽屉里的图片不再被忽略——扫描时与视频一并收进来，共用目录、分组、搜索、排序、星标、重命名、删除与缩略图缓存；新增媒体类型分段器、图片查看器（滚轮翻页 / Ctrl+滚轮缩放 / 幻灯放映 / EXIF 信息面板）、Feed 图片幻灯片、仅图片模式的瀑布流、「按拍摄时间」排序。仍是零依赖单文件，EXIF 解析手写、不读 GPS。设计与分期见 [IMAGE_DESIGN.md](IMAGE_DESIGN.md)。
>
> **关于「⬇️ 下载」页（v1.3 已落地）**：它没有给随拾加后端——下载由同一台机器上已经常驻的拾光笺（Glean）完成，随拾只通过 `fetch` 与 `127.0.0.1` 对话，连不上就整页降级为引导卡。这是对第 1 条红线的一次有意识的绕行：随拾仍是单文件、零构建、双击即开；代价是它第一次会主动发起（仅限本机的）网络请求。设计与边界见 [DOWNLOAD_DESIGN.md](DOWNLOAD_DESIGN.md)。

---

## 实施路线时间表建议

| 阶段 | 周期建议 | 交付物 | 达成效果 |
| :--- | :--- | :--- | :--- |
| **Sprint 1 (P0)** | 1~2 天 | `index.html` 更新 | 播放器倍速、Feed 进度条、快捷键全开、一键刷新、格式识别友好拦截 |
| **Sprint 2 (P1)** | 2~3 天 | `index.html` 更新 | IndexedDB 海报持久缓存、海量视频秒开、断点续播 |
| **Sprint 3 (P2)** | 2 天 | `index.html` 更新 | ⭐ 星标收藏体系、胶囊栏一键筛选精选、废片轻度清理 |
